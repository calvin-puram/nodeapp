pipeline {
    agent any

    stages {
        stage('test-app'){
            steps {
                echo "running app test..."
            }
        }
        stage('deploy-app'){
            steps {
                echo "deploying app..."
            }
        }
    }
}